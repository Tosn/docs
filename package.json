{
  "name": "vite-docs",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "vitepress": "^0.22.3"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs",
    "deploy": "vitepress build docs && bash ./deploy.sh"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
